<%= form_for :patient, url: app_sites_search_path do |form| %>
  <div class="form-group">
    <%= form.text_field :no, class: 'form-control', placeholder: 'Nomor Pasien' %>
  </div>

  <div class="form-group">
    <%= form.submit 'Submit', class: 'btn btn-success' %>
  </div>
<% end %>
